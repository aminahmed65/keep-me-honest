{
  "name": "keep-me-honest",
  "version": "3.0.0",
  "description": "Track promises you make in conversation — Electron menu bar app",
  "main": "dist/main/app.js",
  "scripts": {
    "postinstall": "electron-rebuild -f -w sherpa-onnx-node",
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "tsc && electron ."
  },
  "dependencies": {
    "sherpa-onnx-node": "^1.11.0"
  },
  "devDependencies": {
    "@nicolo-ribaudo/chokidar-2": "^2.1.8-no-fsevents.3",
    "@types/node": "^20.11.0",
    "electron": "^33.2.0",
    "electron-rebuild": "^3.2.9",
    "typescript": "^5.5.0"
  },
  "build": {
    "asarUnpack": [
      "node_modules/sherpa-onnx-node/**",
      "node_modules/sherpa-onnx-darwin-arm64/**"
    ]
  }
}
